<div class="d-flex align-items-center justify-content-center">
  <h1>Welcome</h1>
</div>

<% if @user %>
  Logged in as: <%= @user.email %>
  <%= button_to "Logout", :logout, method: :delete, class: "btn btn-primary" %>
<% end %>
